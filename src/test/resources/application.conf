http : {
    host: "localhost"
    port: 8080
}

db: {
    driver : "org.h2.Driver",
    url: "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;NON_KEYWORDS=USER",
    user: "",
    password: "",
    poolName: "h2-pool",
    maxPoolSize: 1
    threadPoolSize: 1
   # schema : "public"
}